

1. 버튼
버튼의 종류에는 List Button과 Form Button이 있다.
페이지 이동 및 jQuery Mobile 요소를 위한 버튼을 Link Button이라고 하며
<a>태그에 data-role="button"> 속성을 이용해서 처리한다. (하이퍼링크)
일반적인 버튼 사용을 위해서는 <input>태그를 사용한 Form Button을 사용하게 된다.
사용하는 방식이 고정된 것은 아니며 Link Button도 일반적인 형태의 버튼으로
사용 가능하고, Form Button도 링크 동작으로 만들 수 있다.